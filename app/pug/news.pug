extends ./tpl/_layout.pug

block variables
    -
        var title = 'Новости'
        var description = 'Новости'
        var index = false


include ./components/_breadcrumbs.pug
include ./components/_selector


block content 
    -
        var sort = {
            name: 'sort',
            title: '',
            arProperties: [
                {
                    title: '',
                    value: 'price',
                    check: 'По цене'
                }, {
                    title: '',
                    value: 'names',
                    check: 'По названию'
                }, {
                    title: '',
                    value: 'popular',
                    check: 'По популярности'
                }
            ],
            сhecked: 'Сортировка'
        }

    +breadcrumbs(['Главная', 'Новости'], false)

    .container
        .row.row--space-b.row--align-c
            h1 Новости
            +selector(sort.title, sort.name, sort.arProperties, sort.сhecked, 'selector-wrap--absolute')
        
        include ./pages/news/news-content
            